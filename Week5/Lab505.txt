import java.util.Scanner;

public class Lab505 {

	static String studentId;
	static String subjectId;
	static Scanner scan = new Scanner (System.in);
	static void main(String[] args) {
		inputStudent();
	}
	static void inputStudent () {
		while (true) {
			System.out.println("Input Student ID");
		    String studentId = scan.nextLine();
		    System.out.println("Input Subject ID");
		    String subjectId = scan.next();
		    isLength(studentId,subjectId);
		    if (isLength(null, 0) == true) break;
		}
		isITStudent(studentId);
		isITsubject(subjectId);
		
		
	}
	static boolean isLength(String studentId,String subjectId) {
		if (studentId.length() == 10 &&  subjectId == 7) return true;
		else return false;
		
	}
	static boolean isITStudent (String studentId) {
		if (studentId.charAt(2))&&studentId.substring(3,6)) return true;
		else return false;
		
	}
	static boolean isITsubject (String subjectId) {
		if (subjectId.startsWith()) return true;
		else return false;
		
	}
	static void displayData () {
		if(){
			System.out.print("Student id : "+studentId+"1st year student in IT and Enroll in courses for year 1");
		}
	}

}